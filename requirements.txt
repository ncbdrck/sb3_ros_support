# SB3 Requirements
#
# Install with: python3 -m pip install -r requirements.txt

# stable-baselines3 — 2.0+ is Gymnasium-only; 1.8.0 is the last gym
# release if you need to downgrade for legacy envs.
stable_baselines3==2.2.1

# Numerical + config
numpy==1.23.3
pyyaml

# RL API
gymnasium==0.29.1
gymnasium-robotics==1.2.4

# Torch + logging
torch>=2.1.0
tensorboard

# Optional cloud logging. Uncomment to install; the code lazy-imports
# wandb and only activates when a config sets use_wandb: True, so this
# stays a soft dependency. Tested with 0.16.5.
# wandb>=0.16.5
